<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @company.name %>
</p>

<%= render 'complaints/form', complaint: @complaint %>

<% @company.complaints.each do |complaint| %>
  <%= complaint.content %>
  <%= complaint.user.name %>
<% end %>


<%= link_to 'Edit', edit_company_path(@company) %> |
<%= link_to 'Back', companies_path %>
